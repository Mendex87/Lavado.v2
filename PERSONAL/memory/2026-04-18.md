# 2026-04-18

- Se consolidó el despliegue híbrido operativo: app/backend en VPS (contenedor) y `plc_poller` en PC del taller, conectados por Tailscale.
- Se validó conectividad real PC taller -> VPS por Tailscale usando `http://100.99.95.109:42110/plant-api/health` (OK).
- Se confirmó flujo end-to-end con PLC real: `PLC S7-1200 (192.168.10.77, rack 0, slot 1) -> poller Python -> /measurements/ingest -> backend` con lecturas persistidas.
- Se resolvió proxy de Hostinger/OpenClaw para exponer:
  - `/plant-api/*` -> backend FastAPI
  - `/plant-preview/*` -> frontend preview
- Se detectó y corrigió múltiples desfasajes de frontend/backend para lecturas reales:
  - reemplazo de datos mock/fallback por lecturas PLC cuando existan
  - corrección de uso de parcial vs totalizador en dashboard
  - integración de `l1_input_tph` para caudal real L1
- Se estableció actualización periódica:
  - frontend auto-refresh cada 1 segundo
  - poller configurable a 1 segundo (`PLC_POLL_INTERVAL_SECONDS=1`)
- Se acordó regla visual de operación: cuando una línea no tiene proceso activo, su tarjeta del dashboard debe verse “apagada”/parada.
- Branding de frontend actualizado a "Prosil - Lavado" y regla explícita de versionar visualmente cada cambio de frontend.
- Punto clave de negocio/UI para Línea 2:
  - modo simple: lógica equivalente a una sola alimentación
  - modo blend: protagonista debe ser suma de alimentaciones A+B y mostrar además valores individuales de balanza 2 y 3 (caudal y parcial).
- Mapping PLC acordado para caudales en `DB14` (Real):
  - byte 0: L1 caudal
  - byte 4: L2 caudal A
  - byte 8: L2 caudal B
  - byte 12: reservado/futuro
- Riesgo recurrente observado: caché del navegador y/o backend sin reinicio puede ocultar cambios; en pruebas remotas se usa `Ctrl+F5` y, cuando hay cambios backend, reinicio de contenedor.

- Se iteró fuerte la UI del dashboard: se priorizó la alimentación por línea (L1/L2), se separaron salidas/productos en bloque inferior y luego se reemplazó la barra de escala por minigráfico lineal temporal (sparkline) para tendencia de caudal.
- Quedó acordado frenar cambios de dashboard por ahora y pasar a ajustes de OpenClaw.
- Se actualizaron fallbacks del gateway a familia Nexos GPT-5.4: `Nexos GPT 5 4`, `Nexos GPT 5 4 Mini`, `Nexos GPT 5 4 Nano`.
